name: SICOES - Scraping Diario

on:
  schedule:
    # Cada 6 horas: 12am, 6am, 12pm, 6pm (hora Bolivia)
    - cron: '0 4,10,16,22 * * *'
  
  workflow_dispatch:
    inputs:
      limite:
        description: 'Número máximo de convocatorias (default: 20)'
        required: false
        default: '20'

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps
      
      - name: Install xvfb
        run: sudo apt-get update && sudo apt-get install -y xvfb
      
      - name: Run scraper
        run: |
          # Delay aleatorio inicial (0-120 segundos) para parecer humano
          sleep $((RANDOM % 120))
          
          xvfb-run --auto-servernum --server-args="-screen 0 1280x720x24" \
            node descargar.mjs \
            --limit ${{ github.event.inputs.limite || '20' }} \
            --solo-dbc \
            --delay 8000 \
            --out ./descargas
      
      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add descargas/
          git status
          git diff --staged --quiet || git commit -m "📊 Scraping $(date +'%Y-%m-%d %H:%M')"
          git push
